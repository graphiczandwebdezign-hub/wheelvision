export default function AdminPage() {
  return (
    <main className="min-h-screen bg-slate-950 p-8 text-slate-100">
      <div className="mx-auto max-w-4xl rounded-3xl border border-slate-800 bg-slate-900/80 p-8">
        <p className="text-sm uppercase tracking-[0.35em] text-cyan-400">Admin workspace</p>
        <h1 className="mt-3 text-3xl font-semibold">Upload and publish vehicle and wheel packages</h1>
        <p className="mt-4 text-slate-400">
          This scaffold establishes the admin entry point for the metadata-first workflow described in the architecture spec.
        </p>
      </div>
    </main>
  );
}
